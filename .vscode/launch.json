{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run tests",
            "type": "go",
            "request": "launch",
            "mode": "test",
            "program": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}